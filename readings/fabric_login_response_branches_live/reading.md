Read only `6.2.2.3 Responses to Explicit Fabric Login` in `FC-LS.pdf` and extracted the response branches for transmitted FLOGI. The section enumerates: normal Fabric `LS_ACC`; point-to-point `LS_ACC` from another Nx_Port; `F_BSY`; `P_BSY`; `F_RJT` with actions for reason codes `Class not supported` and `Invalid S_ID`; `P_RJT` with action for reason code `Class not supported` and generic handling for other reason codes; `LS_RJT` with D_ID consequences and retry after altering Service Parameters; `No Response` with FC-FS-2 error recovery and possible new FLOGI; and self-connection where equal `N_Port_Name` causes the FLOGI to be discarded. Included D_ID/S_ID consequences where explicitly stated.